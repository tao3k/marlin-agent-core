id = "marlin-workspace-view.scenario-performance"
title = "marlin-workspace-view scenario performance baseline"
policy_ids = ["RUST-AGENT-SCENARIO-PERFORMANCE-001"]
agent_goal = "Keep marlin-workspace-view crate-local scenario performance contracts stable and replayable."
reference_repositories = ["rust-lang/rust", "tokio-rs/tokio"]
reference_patterns = ["scenario benchmark contract", "bounded runtime performance"]
inputs = "inputs"
expected = "expected"
