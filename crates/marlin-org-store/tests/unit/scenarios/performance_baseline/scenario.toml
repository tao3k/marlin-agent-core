id = "marlin-org-store.scenario-performance"
title = "marlin-org-store scenario performance baseline"
policy_ids = ["MARLIN-SCENARIO-PERFORMANCE"]
agent_goal = "Keep marlin-org-store crate-local scenario performance contracts stable and replayable."
inputs = "inputs"
expected = "expected"
