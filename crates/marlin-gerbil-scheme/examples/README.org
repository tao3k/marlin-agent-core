#+title: Marlin Gerbil Scheme Examples

* Boundary

This directory contains downstream-facing examples and fixtures only.
It must not contain the Marlin-owned config interface. The real
Marlin-maintained policy authoring entrypoint for AGENT and human operators
lives at:

#+begin_src text
../gerbil/src/config-interface
#+end_src

Use =user-interface-module-config/= as the downstream user-interface example.
Use =../gerbil/src/config-interface/= when editing Marlin kernel policy packs.
