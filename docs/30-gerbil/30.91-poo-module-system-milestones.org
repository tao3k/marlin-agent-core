* 30.91 POO Module System Milestones
:PROPERTIES:
:ID: docs-30-91-poo-module-system-milestones
:KIND: implementation_plan
:STATUS: active
:END:

This document turns [[file:gerbil-poo-modules-system-desgin.org][Gerbil POO Flow Modules System Design]] and
[[file:30.90-poo-module-system.org][30.90 POO Module System]] into an executable milestone flow.
The current implementation proves the shape and now treats poo-flow
=:poo-flow/src/module-system/facade= as the dependency-owned module system. Marlin provides
=:marlin/modules/prefabs/default-policy= and
=:marlin/modules/prefabs/user-interface= furnished adapter paths with apply,
delivery receipt, catalog presentation, and policy projection handoff. The
broader policy surface still needs more domain prefab packs and custom debug
fact emission.

The milestone model is a trophy-gate flow. A milestone is complete only when it
has user-facing API shape, Scheme receipts, Rust projection behavior, debug CLI
evidence, and docs that tell agents what not to infer.

** 2026-06-17 Module-System Checkpoint
This checkpoint keeps the package/module layer separate from native AOT staging:
- =build.ss compile= runs the ordinary Gerbil package modules and Marlin prefab
  adapters while resolving the module-system implementation from the poo-flow
  dependency. It no longer lists native C ABI modules as =gsc:= entries that
  force =std/make= to look for =*.scm= sources during package compile.
- =build.ss stage-native-aot <deck-runtime|agent-policy-routing> <link-root>= remains the only profile-specific native AOT source mapping.
- The package checkpoint was verified with:
#+begin_src sh
gxpkg deps -i
gxi build.ss compile
gxtest t/marlin-policy-pack-inventory-test.ss
gxtest t/marlin-user-interface-prefab-test.ss
cd ../examples/user-interface-module-config && GERBIL_LOADPATH=../../gerbil/src:../../gerbil/.gerbil/lib:.:t gxtest t/user-interface-module-config-test.ss
#+end_src
- The downstream user-interface module-config test emits =user-interface-script-workflow-ok=, continuation kind =continue_with_graph=, script metrics, and interface-file/state-file checks. That is the current real test that prefab module evaluation, policy-pack object surgery, script execution, continuation projection, and Rust-facing projection receipts stay aligned.
** Operating Rule

Every milestone must preserve this boundary:

- Scheme owns module imports, POO object composition, option merge, policy
  objects, prefab packs, matcher/selector logic, and module evaluation receipts.
- Rust owns typed projection validation, runtime lifecycle, process/tool
  execution, performance budgets, replay, and runtime catalog handler lookup.
- Rust does not parse Scheme source, evaluate imports, merge options, or
  manufacture hook handlers.
- Scheme does not serialize native runtime internals or create Rust handlers
  from text.

** Milestone Map

*** M0 Baseline: Module System Skeleton

Status: landed, but should remain guarded.

Purpose:
- Keep poo-flow =:poo-flow/src/module-system/facade= as the dependency-owned module-system
  library.
- Keep =src/marlin= focused on runtime, debug, policy primitives, prefab
  adapters, and projection glue.
- Keep downstream examples on semantic local names such as
  =user-interface-modules= instead of a second ambiguous =modules= directory.

Required evidence:
- =/usr/local/bin/gxi t/all-test.ss=
- =marlin gerbil policy-receipt= debug CLI output includes =.gerbil/lib=
  between =src= and =t=, proving poo-flow is resolved through =gerbil.pkg=.
- =asp gerbil-scheme check --changed .=

Exit condition:
- Existing tests and docs no longer describe Marlin as owning the module-system
  implementation.

*** M1 Furnished Prefab Pack

Status: landed for the shared =:marlin/modules/prefabs/default-policy= pack and
the =:marlin/modules/prefabs/user-interface= delivery path; additional domain
packs remain an implementation priority.

Purpose:
- Make the default path a furnished house: users import a prefab pack and
  provide only a small config object.
- The pack should include default workspace, agent, hook, model route,
  continuation, review, failure recovery, and catalog projection defaults.
- Users should not start by assembling raw =marlinPolicyModule= objects.

Deliverables:
- =marlinDefaultPolicyPack= and =:marlin/modules/prefabs/default-policy= as the shared
upstream prefab constructor.
- =UserInterfaceWorkspace= from =:marlin/modules/prefabs/user-interface= as the first
concrete furnished-house user API.
- A pack catalog presentation that can list available pack ids, policy
families, default objects, disabled objects, and allowed Rust catalog ids.
- =UserInterfaceDeliveryReceipt= shows the pack id, imported module summary,
object ids, option contracts, operation counts, owner boundaries, and projection
target.

Required evidence:
- Gerbil unit tests for pack catalog listing and default object inventory.
- Debug CLI fields for default pack id, object count, and receipt replayability.
- User-interface example updated to start from =init.ss= plus
  =custom/marlin-user-interface/config.ss= before showing low-level
  construction.

Exit condition:
- A new user can run the furnished user-interface path with only
=UserInterfaceWorkspace=, =UserInterfaceApply=,
=UserInterfaceDeliveryReceipt=, and =UserInterfacePolicyProjection=, without
directly calling =marlinPolicyModule=, =marlinModuleCatalog=, or raw evaluation
helpers.

*** M2 Object Surgery As First-Class User API

Status: partially landed; operation constructors, conflict receipts, and
presentation tests are present for the pack path.

Purpose:
- Make add/remove/disable/replace policy object operations the normal
  customization surface.
- Treat policy objects as furniture: they have family, id, owner, metadata, and
  receipt trail.

Deliverables:
- Stable operation constructors for add/remove/disable/replace.
- Conflict receipts for duplicate object ids, missing target ids, invalid family
  replacement, and forbidden Rust catalog ids.
- A simple public example that replaces a model route and disables human review.

Required evidence:
- Tests proving operation order is deterministic.
- Tests proving disabled objects remain visible in presentation receipts.
- Tests proving Rust catalog ids are checked against an allowed list but not
  resolved by Scheme.

Exit condition:
- Users can customize a prefab pack by object identity without editing option
  priority rules or rewriting module imports.

*** M3 Projection Chain Receipts

Status: landed for the debug policy path, the user-interface delivery receipt,
the user-authored =.ss= entrypoint/call loader, and the shared
=marlinPolicyProjectionChainReceipt= helper.

Purpose:
- Make the module -> policy projection -> native payload chain fixed and
  inspectable for every policy family.

Deliverables:
- Standard receipt names for module evaluation, policy projection, native
  projection payload, budget receipt, and catalog resolution receipt.
- A helper that builds this chain for every prefab and custom policy pack.
Current Gerbil surface: =marlinPolicyProjection= and
=marlinPolicyProjectionChainReceipt=.
- Tests for replayable receipts when no live LLM or runtime catalog lookup is
  allowed.

Required evidence:
- Debug CLI emits all five receipt families.
- Gerbil tests assert owner fields: Gerbil/POO for composition, Rust for native
  projection, budget, and catalog resolution.
- Current proof command: =marlin gerbil policy-receipt= emits
=policy_projection_*= and =policy_projection_chain_*= facts for the debug
prefab pack.
- Current loader proof: the same command accepts =--entrypoint <scheme-file>=
  and =--call <scheme-expr>=, so a newly written =.ss= file can define POO
  objects before delegating or emitting projection facts.

Exit condition:
- Adding a new policy family does not require a new Rust-side policy DSL or
  custom text decoder.

*** M4 Policy Family Expansion

Status: landed for the furnished default pack families; broader domain packs
such as tool batch, self-evolution, and context compression remain future
extensions.

Purpose:
- Turn the design examples into concrete upstream module packs instead of
  scattered hand-built objects.

Policy families:
- workspace policy; current surface: =marlinWorkspacePolicy= and
  =marlinDefaultWorkspacePolicy=;
- session policy; current surface: =marlinSessionPolicy= and
  =marlinDefaultSessionPolicy=;
- agent policy; current surface: =marlinAgentPolicy= and
  =marlinDefaultAgentPolicy=;
- hook selection policy; current surface: =marlinHookSelectionPolicy= and
  =marlinDefaultHookSelectionPolicy=;
- model route policy; current surface: =marlinModelRoutePolicy= and
  =marlinDefaultModelRoutePolicy=;
- continuation profile policy; current surface:
  =marlinContinuationProfilePolicy=, =marlinContinuationProfile=, and
  =marlinDefaultContinuationProfilePolicy=;
- human review policy; current surface: =marlinHumanReviewPolicy= and
  =marlinDefaultHumanReviewPolicy=;
- evidence graph policy; current surface: =marlinEvidenceGraphPolicy= and
  =marlinDefaultEvidenceGraphPolicy=;
- failure recovery policy; current surface: =marlinFailureRecoveryPolicy= and
  =marlinDefaultFailureRecoveryPolicy=;
- memory trigger policy; current surface: =marlinMemoryTriggerPolicy= and
  =marlinDefaultMemoryTriggerPolicy=;
- catalog projection policy; current surface: =marlinCatalogProjectionPolicy=
  and =marlinDefaultCatalogProjectionPolicy=.
Future family candidates:
- tool batch policy;
- self-evolution policy;
- context compression policy;
- subagent policy.

Deliverables:
- One module or object family per policy family.
- A default object for each family in the furnished prefab.
- A minimal override example for each family.

Required evidence:
- Unit tests for default family object constructors and receipt projection.
- One downstream example that composes the default furnished families plus
  user-interface object surgery through one prefab pack.

Exit condition:
- The default pack reads like a real policy surface, not a single debug fixture;
  its default objects are produced by upstream family constructors rather than
  anonymous inline =marlinPolicyObject= furniture.

*** M5 Rust Projection And Debug CLI Gate

Status: partially landed, with typed Rust decoding for default policy delivery
facts and projection-chain facts.

Purpose:
- Keep Rust as the proof boundary: it validates typed projections and reports
  performance/receipt facts without learning Scheme merge semantics.

Deliverables:
- Debug CLI subcommands for pack inventory, object surgery preview, projection
  chain, receipt replay smoke, and performance loop metrics.
- Typed Rust structs for the stable debug/projection facts.

Required evidence:
- =cargo test -p marlin-agent-core --test unit_test runtime::facade::debug_cli
  --locked=
- Real =marlin gerbil policy-receipt= invocation.
- Output includes scheme loop timing and Rust-owned catalog/budget fields.

Exit condition:
- Debug CLI is useful for verifying a newly written =.ss= policy object without
  asking users to inspect internal Scheme values.

*** M6 Downstream Example And Documentation Gate

Status: partially landed.

Purpose:
- Keep the user-facing example aligned with how users should actually write
  policy modules.

Deliverables:
- =custom/marlin-user-interface/config.ss= starts from the poo-flow =load!= user-interface path.
- A second example shows advanced rough-house construction for framework
  authors.
- Docs explain the four user levels: prefab, normal config, object surgery, and
  framework authoring.

Required evidence:
- Example test with semantic loadpath:
  =../../gerbil/src:../../gerbil/.gerbil/lib:t=.
- README and design docs use the same directory names and ownership terms.

Exit condition:
- The example teaches the preferred API first and the low-level API second.

*** M7 CI And Release Gate

Status: missing.

Purpose:
- Make the milestone flow enforceable.

Deliverables:
- A stable Gerbil test command set for local and CI runs.
- A debug CLI smoke that runs with real =gxi= when available.
- A release checklist for module-pack compatibility.

Required evidence:
- =gxi build.ss=
- =gxi build.ss compile=
- =gxi t/all-test.ss=
- =asp gerbil-scheme check --changed .=
- Rust debug CLI tests.

Exit condition:
- A milestone cannot be marked complete without commands that agents can rerun.

** Working Flow

1. Start from the design boundary.
   Check whether the change belongs to Scheme policy/module composition or Rust
   projection/runtime validation.

2. Write the user-facing shape first.
   A milestone must state what the user writes in =.ss= before adding helper
   internals.

3. Add the POO object or module surface.
   Prefer native POO slot composition, =.+=, =.mix=, and inherited slot methods.
   Do not add a second module evaluator.

4. Add receipts before runtime behavior.
   Every merge, object surgery operation, projection, budget decision, and
   catalog decision needs a receipt path.

5. Add Rust only at the typed boundary.
   Rust gets typed facts and projections. It validates, budgets, decodes,
   supervises, and resolves catalog ids.

6. Prove through debug CLI.
   Every milestone should have at least one command that shows the chain from
   Scheme policy object to Rust-consumable facts.

7. Update docs and examples in the same slice.
   A milestone that changes user API must update the furnished-house story,
   example command, and agent-facing "do not infer" boundary.

** Slice Template

Each implementation slice should record:

- Milestone:
- User-facing API:
- Scheme owner:
- Rust owner:
- Receipts added or changed:
- Debug CLI evidence:
- Gerbil tests:
- Rust tests:
- Documentation touched:
- Known gaps carried forward:

** Near-Term Priority

The next three implementation slices should be:

1. Custom pack projection facts:
   Extend the user-authored =.ss= loader so the loaded file defines a full custom
   pack and emits its own pack id, object ids, operation receipts, projection
   chain, owner split, and replayability facts.

2. M5 typed decode:
   Add Rust typed structs for pack inventory, object surgery receipts, projection
   chain facts, owner split, replayability, and timing.

3. Family override examples:
   Add maintained examples that replace model route, hook selection,
   continuation profile, human review, and memory trigger objects through
   family constructors instead of raw =marlinPolicyObject= calls.
* Current 1-6 Implementation Checkpoint
Status: active landing slice.
Scope:
- M1 now includes the thin =UserInterface= downstream entrypoint on top of =UserInterfaceWorkspace=, apply, delivery receipt, and policy projection.
- M2 remains the object-surgery API: users add, remove, disable, and replace named POO policy furniture instead of fighting option priorities.
- M3 remains the fixed module -> projection -> Rust receipt chain; user-authored =.ss= files must emit typed facts instead of Scheme source text.
- M4 now treats memory as four policy families: recall, trigger, retention, and visibility.
- M5 must prove the slice with the real =marlin gerbil policy-receipt= debug CLI, not just direct Scheme tests.
- M6 must keep =user-interface-workspace= thin: the user file starts with a small config and the upstream prefab does the furnished work.

Evidence gate:
- =gxpkg deps -i=
- =gxi build.ss compile=
- =gxi t/marlin-policy-pack-inventory-test.ss=
- =gxi t/marlin-user-interface-prefab-test.ss=
- =cd ../examples/user-interface-module-config && GERBIL_LOADPATH=../../gerbil/src:t gxtest -v t/user-interface-module-config-test.ss=
- =marlin gerbil policy-receipt= with a real =gxi= and a package-local
  =.gerbil/lib= dependency loadpath entry.
